# Procédure création `.env` réel

1. Copier `.env.example` vers `.env` uniquement en local ou dans Netlify UI.
2. Remplacer les placeholders uniquement lorsque le backend sera réellement activé dans une phase future.
3. En v9.10, conserver tous les flags à `false`.
4. Ne jamais commiter `.env`.
