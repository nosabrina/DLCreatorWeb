(function(window){'use strict';const root=window.DLCreatorCore=window.DLCreatorCore||{};const KEY='DL_CREATOR_WEB_SYNC_QUEUE_V1';function queue(){try{return JSON.parse(localStorage.getItem(KEY)||'[]');}catch{return []}}function save(q){try{localStorage.setItem(KEY,JSON.stringify(q.slice(-500)));}catch{}}function enqueue(type,payload){const q=queue();q.push({id:Date.now()+'-'+Math.random().toString(36).slice(2),type,payload,checksum:root.storageAdapter?.checksum?.(payload)||'',createdAtUTC:new Date().toISOString(),retryCount:0,status:'pending'});save(q);return q.length;}root.syncService={enabled:false,remoteEnabled:false,queue,enqueue,clear(){save([]);},retryPrepared:true,diagnostic(){return{enabled:false,remoteEnabled:false,pending:queue().filter(x=>x.status==='pending').length,prepared:true}}};})(window);
