(function(window){'use strict';const root=window.DLCreatorCore=window.DLCreatorCore||{};const KEY='DL_CREATOR_NOTIFICATIONS_V860';const TYPES=['validation-requise','validation-refusée','publication-validée','publication-bloquée','conflit-détecté','ownership-modifié','session-expirée','permissions-insuffisantes','diagnostic-sécurité','backup-terminé','restauration-terminée','workflow','info'];function list(){try{return JSON.parse(localStorage.getItem(KEY)||'[]');}catch{return[]}}function persist(rows){try{localStorage.setItem(KEY,JSON.stringify(rows.slice(-300)));}catch{}}function push(type,payload){const rows=list();const t=TYPES.includes(String(type||''))?String(type):'info';const item={id:'ntf-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,6),type:t,title:payload?.title||'Notification',message:payload?.message||'',dlId:payload?.dlId||'',read:false,priority:payload?.priority||(['publication-bloquée','conflit-détecté','permissions-insuffisantes'].includes(t)?'haute':'normale'),createdAtUTC:new Date().toISOString(),emailPrepared:true,serverPushPrepared:true,webhookPrepared:true,institutionalBroadcastPrepared:true};rows.push(item);persist(rows);return item;}function markRead(id){const rows=list();rows.forEach(n=>{if(n.id===id)n.read=true;});persist(rows);}function diagnostic(){const rows=list();return{total:rows.length,unread:rows.filter(n=>!n.read).length,types:TYPES.reduce((a,t)=>(a[t]=rows.filter(n=>n.type===t).length,a),{}),emailPrepared:true,webhookPrepared:true,serverPrepared:true,offlineFirst:true};}root.notificationService={types:TYPES,list,push,markRead,diagnostic};})(window);
