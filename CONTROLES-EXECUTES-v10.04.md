# Contrôles exécutés — v10.04

- Contrôle syntaxique `node --check app.js`.
- Vérification version v10.04 dans runtime/config/version/index/VERSION.
- Vérification buildIncrement 204.
- Vérification présence flags v10.04.
- Vérification présence classe PDF `pdf-public-cible-v1004` en 10 pt.
- Vérification absence de rétrogradation automatique dans `renderValidation()`.
- Vérification présence actions audit `filRougeImageReordered` et `dlValidationStatusPreservedOnImport`.
