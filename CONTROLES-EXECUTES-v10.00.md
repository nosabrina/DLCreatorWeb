# Contrôles exécutés — v10.00

- Contrôle syntaxique `app.js` via `node --check`.
- Contrôle présence version v10.00 dans `index.html`, `app.js`, `VERSION.md`, `CHANGELOG.md`, `config/runtime-config.js`, `config/app-config.js`, `config/feature-flags.js`, `js/config/version.js`.
- Contrôle cache busting `20260512-v10.00-filrouge-section-images-gallery-pdf`.
- Contrôle absence de référence résiduelle au build v9.13 dans les fichiers principaux.
- Contrôle structure ZIP livrable : `DLCreatorWeb v10.00.zip`.

À valider côté navigateur : Safari localhost, Chrome localhost, ajout/suppression/légende images, sauvegarde/rechargement, import/export JSON, export PDF.
